<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% if (subtitle !== '') { %>
        <title>Zoe's Blog|<%= subtitle %></title>
    <% } else { %>
        <title>Zoe's Blog</title>
    <% } %>
    <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
    <% if (errMessage.length > 0) { %>
        <script>alert('<%= errMessage %>');</script>
    <% } %>
    <nav>
        <div class="container">
            <div>
                <a class="logo" href="/">Zoe's Blog</a>
                <a href="/list">文章列表</a>
                <a href="#">分類專區</a>
                <a href="#">關於我</a>
            </div>
            <div>
                <% if (username) { %>
                    <% if (page !== 'admin') { %>
                        <a href="/admin">管理後臺</a>
                    <% } %>
                    <% if (page !== 'post') { %>
                        <a href="/post">新增文章</a>
                    <% } %>
                    <a href="/logout">登出</a>
                <% } else if (page !== 'login') { %>
                    <a href="/login">登入</a>
                <% } %>
            </div>
        </div>
    </nav>
    <section class="banner bg_toblack">
        <% if (subtitle !== '') { %>
            <h1>存放技術之地——<%= subtitle %></h1>
        <% } else { %>
            <h1>存放技術之地</h1>
        <% } %>
        <p>Welcome to my blog</p>
    </section>